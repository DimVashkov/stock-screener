import { ShortNumberCurrencyPipe } from './short-number-currency.pipe';

describe('ShortNumberCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNumberCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
